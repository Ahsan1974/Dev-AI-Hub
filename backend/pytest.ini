[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q --strict-markers --show-capture=no --tb=short
log_cli = false
log_level = WARNING
filterwarnings =
    ignore::DeprecationWarning
